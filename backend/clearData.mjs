import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { await prisma.paiement.deleteMany(); await prisma.log.deleteMany(); await prisma.demandeSIM.deleteMany(); await prisma.client.deleteMany(); console.log('Toutes les demandes, paiements, logs et clients ont été supprimés avec succès.'); } main().catch(console.error).finally(() => prisma.$disconnect());
